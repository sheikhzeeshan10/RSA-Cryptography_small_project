Encryption and breaking scheme used:
1(a)-  Algorithm which chooses the primes for the RSA „randomly”, based on a random sequence of keyboard input,
1(b)-  then breaking an RSA public key by designing an algorithm which tries to factorize the public key by using Dixon’s algorithm 